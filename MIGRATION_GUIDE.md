# دليل التحويل من Electron إلى تطبيق ويب

## 📋 ملخص التغييرات

تم تحويل تطبيق PortMaster DZ بنجاح من تطبيق سطح مكتب (Electron) إلى تطبيق ويب حديث يعمل على أي متصفح.

## 🔄 التغييرات الرئيسية

### 1. **إزالة المكتبات الخاصة ب Electron**
   - ❌ `electron` - تم حذفه
   - ❌ `electron-builder` - تم حذفه
   - ❌ `better-sqlite3` - تم حذفه
   - ❌ `cross-env` - تم حذفه
   - ❌ `concurrently` - تم حذفه
   - ❌ `wait-on` - تم حذفه
   - ❌ `react-scripts` - تم حذفه

### 2. **إضافة مكتبات الويب**
   - ✅ `vite` - بناء التطبيق السريع
   - ✅ `@vitejs/plugin-react` - دعم React
   - ✅ `axios` - تواصل HTTP مع الخادم

### 3. **تحديث البنية الأساسية**

#### أ) ملف الإعدادات (vite.config.ts)
```typescript
// أضيف دعم محاكاة الخادم في البيئة المحلية
proxy: {
  '/api': {
    target: process.env.VITE_API_URL || 'http://localhost:5000',
    changeOrigin: true,
  }
}
```

#### ب) إنشاء ملف API Client جديد (services/api.ts)
- يستخدم Axios للتواصل مع الخادم
- يدعم نقل التوكن (Token) تلقائياً مع كل طلب
- يعيد التوجيه للصفحة الرئيسية عند انتهاء جلسة المستخدم

#### ج) تحديث Backend Service (services/backend.ts)
تم استبدال جميع استدعاءات `window.electronAPI` بـ استدعاءات HTTP:

| العملية | Electron القديم | الويب الجديد |
|---------|-----------------|------------|
| تسجيل الدخول | `electronAPI.login()` | `POST /auth/login` |
| جلب الصيادين | `electronAPI.getAllFishers()` | `GET /fishers` |
| إضافة صياد | `electronAPI.saveFisher()` | `POST /fishers` |
| مسح NFC | `electronAPI.scanNFC()` | `POST /nfc/scan` |
| حفظ التقرير | `electronAPI.savePDF()` | `POST /reports/upload` |

### 4. **تحديث ملف الأنواع (types.ts)**
- تم إزالة تعريف `window.electronAPI` العام
- أضيفت نماذج البيانات الأساسية

### 5. **تحديث سجلات npm**
تم تغيير أوامر تشغيل التطبيق:

```json
{
  "scripts": {
    "start": "vite",           // تشغيل الويب
    "dev": "vite",             // بيئة التطوير
    "build": "vite build",     // بناء الإنتاج
    "preview": "vite preview"  // معاينة النتيجة
  }
}
```

### 6. **ملف البيئة الجديد (.env.example)**
```
VITE_API_URL=http://localhost:5000
VITE_APP_TITLE=PortMaster DZ
```

## 🚀 كيفية التشغيل

### 1. التطوير المحلي
```bash
# تثبيت المكتبات
npm install

# تشغيل الخادم المحلي
npm run dev
```
التطبيق متاح الآن على: `http://localhost:3000`

### 2. البناء للإنتاج
```bash
npm run build
```
المخرجات في مجلد `dist/` جاهزة للنشر على أي خادم.

## 📡 متطلبات الخادم الخلفي

التطبيق يحتاج إلى خادم يدعم الـ API التالية:

### Authentication (المصادقة)
```
POST /auth/login
Body: { email, password }
Response: { token, user: { email, name, role } }
```

### Fishers (إدارة الصيادين)
```
GET    /fishers
POST   /fishers
PUT    /fishers/:id
DELETE /fishers/:id
```

### NFC Operations
```
POST /nfc/scan
POST /nfc/logs
GET  /nfc/logs
```

### Catches (سجل الصيد)
```
GET    /catches
POST   /catches
PUT    /catches/:id
```

### Renewals (التجديدات)
```
GET  /renewals
POST /renewals
```

### Reports (التقارير)
```
POST /reports/upload
```

## 🔐 المزايا الجديدة

✅ **توافقية عالية** - يعمل على جميع المتصفحات الحديثة
✅ **سهل النشر** - لا يحتاج إلى تطبيق منفصل للمستخدمين
✅ **قابل للتوسع** - يمكن تشغيل عدة نسخ بسهولة
✅ **محسّن للأداء** - حجم بناء أصغر من Electron
✅ **أمان أفضل** - التخزين المركزي للبيانات
✅ **وضع بدون إنترنت** - يدعم Mock Mode للاختبار

## ⚠️ ملاحظات هامة

### الميزات المتعلقة بـ NFC
كان التطبيق Electron يتواصل مع قارئ NFC المادي. الآن:
- يتم محاكاة sNFC في وضع التطوير
- يمكن للخادم أن يتعامل مع قارئ NFC الحقيقي

### تخزين البيانات
- **Electron القديم**: SQLite محلي
- **الويب الجديد**: قاعدة بيانات مركزية على الخادم

### وضع بدون إنترنت
التطبيق يدعم Mock Mode:
```typescript
// عند عدم توفر الخادم، يستخدم بيانات وهمية
if (apiError) {
  return mockData;
}
```

## 🛠️ الملفات التي تم تعديلها

1. ✅ `package.json` - تحديث المكتبات والأوامر
2. ✅ `vite.config.ts` - إضافة الإعدادات الملائمة
3. ✅ `services/api.ts` - إنشاء (جديد)
4. ✅ `services/backend.ts` - استبدال Electron IPC بـ HTTP
5. ✅ `types.ts` - إزالة electronAPI
6. ✅ `components/AdminView.tsx` - تحديث التعليقات
7. ✅ `README.md` - تحديث التعليمات
8. ✅ `.env.example` - إنشاء (جديد)

## 📚 المراجع

- [Vite Documentation](https://vitejs.dev)
- [React Documentation](https://react.dev)
- [Axios Documentation](https://axios-http.com)
- [TypeScript Documentation](https://www.typescriptlang.org)

---

**تم التحويل بنجاح! 🎉**

التطبيق الآن جاهز للعمل كموقع ويب حديث مع دعم كامل للميزات السابقة.
